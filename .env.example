# Optional services only. Never place real values in this file.
TRANSCRIPTION_PROVIDER=
TRANSCRIPTION_API_KEY=
AI_LABEL_PROVIDER=
AI_LABEL_API_KEY=
RANDOM_SEED=20260730

# Local / cloud Postgres (never commit real credentials)
# Neon pooled example:
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@ep-xxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql+psycopg://lucy_marketing:lucy_marketing_dev@localhost:5432/video_analytics

# Next.js (web/.env.local)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Pause Safely

Record the active unit, completed steps, changed/uncommitted files, tests actually run and results, blockers, known issues, exact next safe action, and last verified checkpoint in `TASKS.md` and `docs/CoBuildLog.md`. Do not mark the unit complete unless every criterion passed. Do not start another unit.

# INIT - Project Initialization

## Route Warning
Use this prompt exactly once only in a genuinely empty workspace. The packaged starter is already initialized, so do not run INIT against it.

## Objective
Materialize the approved `lucy-short-video-marketing-analytics` structure and foundational control files without implementing research features.

## Required Initialization
Create README, TASKS, prompts, docs, config, public synthetic fixture, package placeholder, initialization smoke test, ignore rules, environment placeholders, dependency manifest, and CI scaffold matching the packaged tree.

## Verification
Print the tree, parse JSON/TOML configuration, run the package import smoke test, scan for secrets/private data, and report files created/preserved.

## Do Not Do
Do not collect real videos, create results, implement S01-S18, add optional expansion/dashboard work, overwrite existing files, or commit secrets/private transcripts.

## Stop Condition
Stop after the initialized structure passes. Do not begin S01.
